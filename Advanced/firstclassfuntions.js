//A language is said to have first class,when the function in that language is treated as variables(normal values), you can save them , you can pass them as arguements to another functions.
//set time interval is an example.

 var abc= function()
 {
    
 }